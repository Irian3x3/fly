export const config = {
    bot: {
        core: {
            token: "", // Bot token, gotten from discord.com/developers/applications
            prefix: "fly@",
            owners: [""] // Your user ID
        }
    }
}